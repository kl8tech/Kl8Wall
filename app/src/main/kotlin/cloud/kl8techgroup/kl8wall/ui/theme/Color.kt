package cloud.kl8techgroup.kl8wall.ui.theme

import androidx.compose.ui.graphics.Color

/** KL8 brand primary — emerald green. */
val Kl8Green = Color(0xFF10B981)

/** Lighter variant for containers and highlights. */
val Kl8GreenLight = Color(0xFF34D399)

/** Darker variant for pressed/active states. */
val Kl8GreenDark = Color(0xFF059669)

/** Primary surface — near-black with a cool tint. */
val Kl8Surface = Color(0xFF0F1419)

/** Elevated surface — cards, sheets, dialogs. */
val Kl8SurfaceElevated = Color(0xFF1A2332)

/** Highest surface elevation — floating elements. */
val Kl8SurfaceTop = Color(0xFF243044)

/** Primary text on dark backgrounds. */
val Kl8OnSurface = Color(0xFFE2E8F0)

/** Secondary/muted text. */
val Kl8OnSurfaceVariant = Color(0xFF94A3B8)

/** Error state. */
val Kl8Error = Color(0xFFEF4444)

/** Error container. */
val Kl8ErrorContainer = Color(0xFF7F1D1D)

/** On-error text. */
val Kl8OnError = Color(0xFFFFFFFF)

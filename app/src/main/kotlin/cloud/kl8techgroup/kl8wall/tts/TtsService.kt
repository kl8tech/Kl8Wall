package cloud.kl8techgroup.kl8wall.tts

/**
 * Wrapper around Android's TextToSpeech engine.
 *
 * Accepts text via the REST API and speaks it through the device speaker.
 * Handles TTS engine initialization and lifecycle.
 * Implemented in Phase 5.
 */
class TtsService
